"""Live smoke tests — hit real public exchange endpoints (network required).

Run with ``pytest -m live tests/live``; excluded from the default test run
by ``addopts = "-m 'not live'"`` in ``pyproject.toml``.
"""
